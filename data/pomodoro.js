export const DEFAULT_MINUTES = 25;
export const DEFAULT_SECONDS = 0;
export const DEFAULT_STATE = 'pomodoro';

export const timer = { state: DEFAULT_STATE, minutes: DEFAULT_MINUTES, seconds: DEFAULT_SECONDS }